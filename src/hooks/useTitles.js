/**
 * This hook returns a list of title objects (title and id) associated with the passed in section. 
 * 
 * This hook also should be listening to changes to the list of titles in the database and updating as appropriate.
 * 
 * @param {string} section 
 * @returns 
 */


function useTitles(section) {
  return [];
}

export default useTitles;
