/**
 * This hook uses the router to determine the correct article to view and fetches it from the database (storing it as local state).
 * 
 * This function returns a two element array. The first element is the current article itself and the second is a setter for the current article (which should set the route, not update internal state).
 * 
 * 
 * 
 * @returns [currentArticle, setCurrentArticle]
 */



export default function useCurrentArticle() {
  
  return [];
}
